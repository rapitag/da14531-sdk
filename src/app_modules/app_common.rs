pub use crate::bindings::{
    app_default_handler, app_device_info as AppDeviceInfo, app_device_name as AppDeviceName,
    app_state, APP_IDX_MAX, APP_STATE_APP_CONNECTABLE as APP_CONNECTABLE,
    APP_STATE_APP_DISABLED as APP_DISABLED, APP_STATE_APP_STATE_MAX as APP_STATE_MAX,
};

pub mod app;
